<!DOCTYPE html>
<html>

<head>
  <title>Update Student</title>
</head>
<style>
  input {
    display: block;
    padding: 10px;
    font-size: 20px;
    width: 400px;
    margin-bottom: 10px;
  }

  h2 {
    font-size: 40px;
    border-bottom: 3px solid rgb(0, 245, 114);
    width: fit-content;
  }

  body {
    margin: 5% auto;
    width: 600px;
  }

  button {
    width: 400px;
    padding: 10px;
    margin: 5% auto;
    font-size: 20px;
    border: none;
    font-weight: 700;
    background-color: rgb(0, 245, 114);
  }
</style>

<body>
  <h2>Update Student</h2>
  <form method="post" action="/update-user/<%= student._id %>" class="details-form">
    <input type="text" name="name" value="<%= student.name %>" required />

    <input type="number" name="rollno" value="<%= student.rollno %>" required />
    <input type="text" name="wad_marks" value="<%= student.wad_marks %>" required />
    <input type="number" name="dsbda_marks" value="<%= student.dsbda_marks %>" required />
    <input type="number" name="cns_marks" value="<%= student.cns_marks %>" required />
    <input type="number" name="cc_marks" value="<%= student.cc_marks %>" required />
    <input type="number" name="ai_marks" value="<%= student.ai_marks %>" required />
    <button>Update</button>
  </form>
</body>

</html>